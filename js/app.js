(function () {
    angular
        .module("turtleFacts", []);
})();



//2nd empty [] in module means we are not fetching turtleFacts module but we are creating it.. it we remove [] it means we are fetching the module